package io.github.droidkaigi.confsched.core.model

/** A topic the conference groups sessions under, named in both of the API's languages. */
data class SessionCategory(val id: Long, val name: MultiLangText)
