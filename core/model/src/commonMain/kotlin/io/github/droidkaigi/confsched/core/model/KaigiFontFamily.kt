package io.github.droidkaigi.confsched.core.model

enum class KaigiFontFamily {
    /** The display face on display and headline roles, the text face on the rest. */
    Default,
    CourierPrime,
    NotoSans,
}
