package io.github.droidkaigi.confsched.core.model

/** A surface the user can doodle on. Each target keeps its own drawing and its own dp space. */
enum class DoodleTarget {
    AboutWall,
    ProfileCardFront,
    ProfileCardBack,
}
