package io.github.droidkaigi.confsched.feature.doodle

import androidx.compose.ui.unit.dp

/** The inset the editor keeps between its content and the edges of the space it is given. */
internal val DoodleEditContentPadding = 16.dp

/** The gap between the editor's own sections: a canvas, the face switch, and the save action. */
internal val DoodleEditSectionSpacing = 16.dp

/** The gap between neighbouring controls within one section. */
internal val DoodleEditControlSpacing = 8.dp
